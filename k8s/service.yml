apiVersion: v1
kind: Service
metadata:
  labels:
    app: blaster
  name: blaster
  namespace: $(kubernetes-namespace)
  annotations:
    scrape-metrics: 'true'
spec:
  ports:
  - name: external
    port: 80
    targetPort: web
  selector:
    app: blaster
  type: ClusterIP